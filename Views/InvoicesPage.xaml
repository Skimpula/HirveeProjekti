<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:HirveeProjekti.ViewModels"
             x:Class="HirveeProjekti.Views.InvoicesPage"
             Title="Laskut">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- 🔹 Otsikko -->
            <Label Text="Laskujen hallinta"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

             <Label Text="Luo lasku varauksesta"
                 FontSize="18"
                 FontAttributes="Bold"/>

             <Picker Title="Valitse varaus"
                  ItemsSource="{Binding AvailableBookings}"
                  ItemDisplayBinding="{Binding InvoiceCreationDisplay}"
                  SelectedItem="{Binding SelectedBookingForInvoice}" />

             <Button Text="Luo lasku (mökin hinta × päivien määrä)"
                  Command="{Binding CreateInvoiceFromBookingCommand}"
                  BackgroundColor="#1976D2"
                  TextColor="White"
                  CornerRadius="8"
                  HeightRequest="45" />

             <Label Text="{Binding InvoiceActionMessage}"
                 TextColor="#1B5E20"
                 IsVisible="{Binding HasInvoiceActionMessage}" />


                <!-- 📋 Lista laskuista -->
                <Label Text="Kaikki laskut"
                   FontSize="18"
                   FontAttributes="Bold"/>

       <!-- RIKKI   
           <HorizontalStackLayout Spacing="10" Margin="0,0,0,10">
                <Button Text="Kaikki"
                    Command="{Binding SetInvoiceFilterCommand}"
                    CommandParameter="All"
                    BackgroundColor="{Binding InvoiceFilter, Converter={StaticResource FilterToColorConverter}, ConverterParameter=All}"
                    TextColor="White"
                    CornerRadius="8" />
                <Button Text="Maksetut"
                    Command="{Binding SetInvoiceFilterCommand}"
                    CommandParameter="Paid"
                    BackgroundColor="{Binding InvoiceFilter, Converter={StaticResource FilterToColorConverter}, ConverterParameter=Paid}"
                    TextColor="White"
                    CornerRadius="8" />
                <Button Text="Maksamattomat"
                    Command="{Binding SetInvoiceFilterCommand}"
                    CommandParameter="Unpaid"
                    BackgroundColor="{Binding InvoiceFilter, Converter={StaticResource FilterToColorConverter}, ConverterParameter=Unpaid}"
                    TextColor="White"
                    CornerRadius="8" />
                </HorizontalStackLayout> -->

            <CollectionView ItemsSource="{Binding FilteredInvoices}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightGray"
                               BackgroundColor="White"
                               Padding="10"
                               Margin="0,5"
                               CornerRadius="5">
                            <VerticalStackLayout Spacing="5">
                                <VerticalStackLayout.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectInvoiceCommand}"
                                        CommandParameter="{Binding .}"/>
                                </VerticalStackLayout.GestureRecognizers>

                                <Label Text="{Binding LaskuId, StringFormat='Lasku ID: {0}'}"
                                       FontAttributes="Bold"
                                       TextColor="Black"/>
                                <Label Text="{Binding VarausId, StringFormat='Varaus ID: {0}'}"
                                       TextColor="Black"/>
                                <Label Text="{Binding Summa, StringFormat='Summa: {0:F2} €'}"
                                       TextColor="Black"
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding Alv, StringFormat='ALV: {0:F2} €'}"
                                       TextColor="Black"/>
                                    <Label Text="{Binding TilaTeksti, StringFormat='Tila: {0}'}"
                                       TextColor="Black"/>

                                <HorizontalStackLayout Spacing="10">
                                     <Button Text="Vaihda maksun tila"
                                          Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.TogglePaymentStatusCommand}"
                                            CommandParameter="{Binding LaskuId}"
                                            BackgroundColor="#4CAF50"
                                            TextColor="White"
                                            CornerRadius="8"
                                            HeightRequest="40"/>

                                    <Button Text="Lähetä muistutus"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SendReminderCommand}"
                                            CommandParameter="{Binding LaskuId}"
                                            BackgroundColor="#FFC107"
                                            TextColor="Black"
                                            CornerRadius="8"
                                            HeightRequest="40"/>
                                </HorizontalStackLayout>

                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- 📄 Valitun laskun tiedot -->
            <BoxView Color="LightGray"
                     HeightRequest="2"
                     Margin="0,10,0,10"/>


                 <Label Text="Valitun laskun tiedot"
                     FontSize="18"
                     FontAttributes="Bold"/>

              

            <Frame BorderColor="LightBlue"
                   Padding="15"
                   Margin="0,5"
                   CornerRadius="8">
                   
                <VerticalStackLayout Spacing="8">
                    <Label Text="{Binding SelectedInvoiceDetails}"
                           LineBreakMode="WordWrap"/>
                              
                    <Button Text="Tulosta PDF"
                      Command="{Binding PrintInvoiceToPdfCommand}"
                      IsEnabled="{Binding IsInvoiceSelected}"
                      BackgroundColor="#607D8B"
                      TextColor="White"
                      CornerRadius="8"
                      HeightRequest="45"
                      Margin="0,0,0,10" />
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>